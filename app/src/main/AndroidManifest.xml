<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vickers.ebook_reader">
    <!--权限列表-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CAppTheme">
        <activity android:name=".View.activites.UserCenterActivity"></activity>
        <!-- 欢迎界面 -->
        <activity android:name=".View.activites.WelcomeActivity"
            android:theme="@style/CAppWelcomeTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <!-- 主界面 -->
        <activity android:name=".View.activites.BookshelfActivity"
            android:theme="@style/BookShelfTheme"
            android:configChanges="locale|keyboardHidden|orientation|screenSize|smallestScreenSize|screenLayout"
            android:launchMode="singleTask"/>
    </application>
</manifest>